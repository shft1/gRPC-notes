Summary:

### Жизненный цикл unary-запросов

- Все четыре типа gRPC запросов (unary, client stream, server stream, bidirectional stream) внутри протокола являются bidirectional streams
- Такая универсификация протокола упрощает инженерную работу и обработку на уровне кода
- gRPC не работает в браузере из-за отсутствия прямого доступа к HTTP/2 в JavaScript
- При создании клиентской структуры фактически уже происходит первый запрос для установки соединения
- За счет мультиплексирования HTTP/2 все запросы проходят через одно единое соединение, что убирает транспортные расходы на handshakes

### Интерцепторы

- Запросы проходят через цепочку интерцепторов как на клиенте, так и на сервере
- Middleware прогоняются не только на сервере, но и на клиенте - ключевое отличие от HTTP запросов
- Клиентские интерцепторы позволяют валидировать запросы до отправки на сервер, снижая нагрузку
- Можно использовать одни и те же интерцепторы на клиенте и сервере, так как сигнатуры идентичны
- Не рекомендуется писать свои интерцепторы - в gRPC ecosystem есть готовые решения для логирования, метрик, трейсинга, авторизации
- Важно не накидывать слишком много логики в интерцепторы, чтобы не создавать излишнюю "магию"

### Сериализация и сжатие

- Protobuf сериализация идет из коробки во всех gRPC запросах
- Flatbuffers - эволюция protobuf от Google, меньше весит и быстрее сериализуется, но меньше экосистема
- Фрейминг необходим для разделения сообщений в рамках одного соединения
- Сжатие рекомендуется включать только для больших сообщений, передаваемых нечасто
- При частых маленьких сообщениях сжатие сильно увеличивает нагрузку на CPU без существенной пользы

### Конфигурация сервера

MaxConnectionIdle

- Контролирует количество неактивных соединений
- Полезно при большом количестве клиентов, которые редко делают запросы
- Пример применения: мобильные приложения такси, использующие gRPC для экономии трафика

MaxConnectionAge

- Регулирует длительность соединения для профилактики деградации
- Обеспечивает ротацию соединений даже если они активны

MaxConnectionAgeGrace

- Минимизирует отмены по ротации, дожидаясь корректного drain соединения
- Гарантирует полную очистку соединения перед отключением

KeepAlive параметры

- Timeout - для разрыва соединений без активности на старте
- Time - сколько времени клиент ждет перед отправкой ping-запроса
- Для backend-to-backend рекомендуется 5-10 минут
- Для мобильных сетей требуется настройка из-за нестабильности соединений

MaxConcurrentStreams

- По умолчанию ограничивает обработку до 100 запросов одновременно
- Можно уменьшить для ограниченных ресурсов (например, на одноядерных процессорах)
- Уменьшение значения также снижает количество garbage collection

### Работа с памятью

- Дефолтное соединение gRPC принимает не больше 4 МБ, максимум можно увеличить до 64 МБ
- Не рекомендуется повышать лимит данных - на этапе десериализации будут попытки выделять память исходя из процента разрешенного лимита
- ReadBufferSize использует четко фиксированное значение (не коэффициент), поэтому его регулирование еще более рискованно
- При передаче больших объемов данных рекомендуется использовать client stream вместо увеличения buffer size

### Передача файлов через gRPC

- Файлы передаются чанками, протокол передачи полностью кастомный
- io.copy не работает эффективно с gRPC (не может использовать Unix-операции для прямого соединения потоков)
- Для очень маленьких файлов можно использовать base64 encoding
- Глобально проблема эффективной передачи файлов через gRPC пока не решена

### Обработка ошибок

Статус-коды

- Статус - краеугольный камень обработки ошибок в gRPC
- Можно мапить gRPC статусы на HTTP статусы (например, Unknown → Internal Server Error)
- Некоторые коды (Unavailable, DeadlineExceeded) могут вызывать автоматический retry на клиенте
- Не рекомендуется добавлять NotFound в retryable коды

Details для детализации ошибок
- Details позволяет расширять информацию об ошибках, передавая дополнительные proto-сообщения
- Вместо возвращения one-of с ошибкой в response следует использовать статус с details
- Можно создавать кастомные error message с enum кодами ошибок для консистентной обработки на клиенте
- Details - это массив, можно передать несколько сообщений об ошибках
- Для разных environments можно включать разную детализацию (на prod не показывать внутренние ошибки)

### Валидация с использованием protovalidate

- Библиотека buf-validate (ранее protovalidate) позволяет декларативно описывать правила валидации в proto-файлах
- Можно указывать минимальную/максимальную длину, количество элементов, регулярные выражения
- Поддерживает сложные правила с зависимостями между полями
- Валидация применима для всех языков, не требует дублирования логики на разных платформах
- На уровне proto следует валидировать только примитивы - сложные бизнес-проверки (уникальность, доступность в БД) делаются в бизнес-логике
- Достаточно вызвать один метод protovalidate.Validate(request) для проверки

### Аутентификация

JWT токены через metadata

- Metadata передается аналогично HTTP заголовкам
- Токены авторизации передаются через metadata с ключом "authorization"
- На сервере токен извлекается из контекста и валидируется в интерцепторе
- На клиенте metadata упаковывается в контекст перед вызовом метода

Современный паттерн с Istio

- Istio может валидировать токен до достижения приложения
- Istio парсит токен и упаковывает данные пользователя в metadata
- Сервис получает готовый объект пользователя из контекста, не занимаясь парсингом токена
- Упрощает написание бизнес-логики в обработчиках

### Валидация с обратной совместимостью

- При переносе валидации на клиентскую сторону важно помнить о обратной совместимости
- Изменение правил валидации может сломать клиентов, которые не обновились
- Рекомендуется иметь валидацию и на клиенте, и на сервере, но не только на клиенте

### Инструменты и экосистемы

- gRPC ecosystem - организация с готовыми инструментами для работы с gRPC
- OpenTelemetry - для работы с трейсами (например, Tempo из Grafana stack)
- Buf - произошла дефрагментация community, Buf вывел свои наработки отдельно
- В gRPC ecosystem есть готовые решения для большинства задач, не нужно писать велосипеды

### Практические примеры

- Реализация статистического интерцептора для логирования времени выполнения запросов
- Создание интерцептора аутентификации с проверкой токена
- Обработка кастомных ошибок с бизнес-кодами
- Интеграция валидации в обработчики запросов

### Рекомендации

- Конфигурации должны подбираться исходя из задач и тестирования
- Дефолтные настройки решают большинство проблем, но требуют регулировки для backend-to-backend соединений
- Некоторые параметры взаимоисключающие или влияют друг на друга
- Zero Trust подход (все запрещено по умолчанию) предпочтителен для безопасности

Домашнее задание:

Задание 1: Интерцепторы (Middleware)

Необходимо реализовать два серверных Unary-интерцептора и подключить их через grpc.ChainUnaryInterceptor.

Logger Interceptor:

- Должен перехватывать запрос
- Логировать начало запроса (Method name)
- Замерять время выполнения хендлера
- Логировать конец запроса (статус ответа + затраченное время)

💡 Совет: используйте time.Now() и time.Since()

Auth Interceptor:

- Должен извлекать метадату из контекста (`metadata.FromIncomingContext`)
- Проверять наличие ключа authorization

Логика:

- Если ключа нет или токен не валиден (например, хардкод Bearer my-secret-token`), возвращать ошибку со статусом `Unauthenticated
- Если токен валиден, пропускать запрос дальше к хендлеру

---

Задание 2: Конфигурация сервера

Вместо дефолтного запуска сервера grpc.NewServer() добавьте опции (`grpc.ServerOption`) для управления соединениями, чтобы защитить сервер от зависших коннектов и перегрузки.

Настройте следующие параметры (значения можно взять примерные, главное наличие конфигурации):

- MaxConcurrentStreams: ограничьте количество одновременных стримов (например, 10-50)
- KeepAlive: настройте параметры Time (время между пингами) и Timeout (время ожидания ответа), чтобы разрывать мёртвые соединения

---

Задание 3: Валидация (Protovalidate)

Внедрите валидацию на уровне .proto файла, чтобы не писать проверки if len(req.Title) == 0 в Go-коде.

Подключите зависимость [`buf.build/bufbuild/protovalidate`](http://buf.build/bufbuild/protovalidate) (или protoc-gen-validate, если используете старый стек, но в лекции рекомендовали современный подход).

Модифицируйте метод CreateNote (или аналогичный):

- Поле title: обязательное, минимальная длина 5 символов, максимальная 255
- Поле content: обязательное, минимальная длина 10 символов
- Поле email (если есть): должно соответствовать формату email

Сгенерируйте код (`easyp generate` или `protoc`).

Вставьте интерцептор валидации или вызовите валидатор внутри хендлера (как было показано в лекции: `validator.Validate(req)`).

---

Задание 4: Rich Error Handling (Детализация ошибок)

Измените возврат ошибок в одном из методов (например, `GetNote`).

Вместо простого status.Errorf(codes.NotFound, "note not found") реализуйте возврат ошибки с деталями (`WithDetails`).

Создайте (или используйте существующий) message в proto-файле для описания ошибки (например, ErrorDetails с полем reason или `internal_error_code`).

Сценарий:

- Если заметка не найдена, верните статус NotFound
- В Details положите сообщение: "Note with ID {id} was searched but not found in DB"

*(Опционально)* Напишите в клиенте код, который проверяет: если пришла ошибка, попробуй достать из неё Details и вывести пользователю красивое сообщение.
